name = "Alice"
coordinate = (10.0, 20.0)               #This is a TUPLE, multiple values are stored in a single name.
names = ["Alice", "Bob", "Charlie"]     #List (array) Store bunch of values all together in one.

print(name[0])
print(coordinate[0])
print(names[0])
